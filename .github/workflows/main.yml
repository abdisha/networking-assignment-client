name: Build Desktop Installers

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # This triggers two separate parallel builds
        os: [windows-latest, ubuntu-latest]

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean package

    - name: Build and Prepare Libs
      run: |
        mvn clean package
        mvn dependency:copy-dependencies -DoutputDirectory=target/libs
        # Move the main jar into the input folder so jpackage finds it
        cp target/networking-assignment-client-1.0-SNAPSHOT.jar target/libs/

    # --- WINDOWS BUILD (.exe) ---
    - name: Create Windows EXE
      if: matrix.os == 'windows-latest'
      run: |
        jpackage --type exe --name "NetworkingChat-client" `
        --input target/libs --main-jar networking-assignment-client-1.0-SNAPSHOT.jar `
        --main-class com.assignment.networkingassignmentclient.MainApp `
        --win-shortcut --win-menu --dest dist/

    # --- UBUNTU BUILD (.deb) ---
    - name: Create Ubuntu DEB
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get install -y fakeroot
        jpackage --type deb --name "networking-chat-client" \
        --input target/libs --main-jarnetworking-assignment-client-1.0-SNAPSHOT.jar \
        --main-classcom.assignment.networkingassignmentclient.MainApp \
        --linux-shortcut --dest dist/


    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: chat-client-${{ matrix.os }}
        path: dist/
